#include <stdio.h>

int main(){
   int n,s=0,p=1,i;
   scanf("%d",&n);
   int a[n];
   for(i=0;i<n;i++){
       scanf("%d",&a[i]);
   }
   for(i=0;i<n;i++){
       s=s+a[i];
       p=p*a[i];
   }
   printf("%d\n%d",s,p);
   
   
    return 0;
}
